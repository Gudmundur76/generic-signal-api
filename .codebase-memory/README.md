# Codebase Memory Graph

Knowledge graph of `Gudmundur76/generic-signal-api` generated by [codebase-memory-mcp](https://deusdata.github.io/codebase-memory-mcp/) v0.8.1.

| Metric | Value |
|---|---|
| Nodes | 1793 |
| Edges | 3961 |
| Compressed size | 1.4MB |
| Last indexed | 2026-06-30 |

## Bootstrap in a new session

```bash
# Install binary (one-time)
curl -fsSL https://raw.githubusercontent.com/DeusData/codebase-memory-mcp/main/install.sh \
  -o /tmp/cbm-install.sh && bash /tmp/cbm-install.sh --skip-config
export PATH="$HOME/.local/bin:$PATH"

# Restore graph
mkdir -p ~/.cache/codebase-memory-mcp
gunzip -c .codebase-memory/graph.db.gz > ~/.cache/codebase-memory-mcp/Gudmundur76-generic-signal-api.db

# Verify
codebase-memory-mcp cli index_status '{"project":"Gudmundur76-generic-signal-api"}'
```

## Key architecture nodes

- `server/lib/autonomousLoop.ts` — core discovery→design→deliver pipeline
- `server/lib/resistAgent.ts` — HIV protease resistance scoring (37 mutations)
- `server/lib/patentArbitrage.ts` — 8-jurisdiction IP gap scorer
- `server/lib/citationClient.ts` — citation verification with PubMed fallback
- `server/routers/partners.ts` — partner management + `recordDelivery` (serviceProcedure)
- `server/_core/trpc.ts` — tRPC core with `serviceProcedure` middleware

## Auto-update

`.github/workflows/codebase-memory.yml` re-indexes on every push to `main`.
